[project]
name = "huggingfacehub"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "huggingface-hub>=1.20",
    "hf-xet",
]

[tool.setuptools]
py-modules = ["hfu", "hf_xfer"]

[project.scripts]
hfu = "hfu:main"
hf-xfer = "hf_xfer:main"
